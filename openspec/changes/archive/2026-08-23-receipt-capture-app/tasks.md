## 1. 契約（両側が依存するので最初に固める）

- [x] 1.1 `packages/shared` にメタ JSON の zod スキーマを定義する（件の識別子・撮影日時・画像 SHA-256・支払手段・用途・飲食の参加人数と相手・摘要・端末 OCR の日付と金額・簡易検査の指摘）
- [x] 1.2 `packages/shared` に status ファイルの zod スキーマを定義する（件の識別子・登録の成否・理由・登録された日付/金額/科目の要約）
- [x] 1.3 zod から JSON Schema を生成してリポジトリに成果物として置くビルド手順を追加する
- [x] 1.4 ゴールデンフィクスチャを 1 セット置く（メタ: 正常系・現金・カード・欠損・不正／status: 完了・重複・未突合・期間外）
- [x] 1.5 TS 側でフィクスチャを読む契約テストを書く（正常系は通り、不正系は zod が弾くことを固定する）

## 2. Mac 側の受け皿（端末を待たずに完成させる）

- [x] 2.1 `POST /skill/receipts/journal-candidates` を追加する（現金レシート → `raw_transactions` ＋ draft 仕訳 ＋ 証憑添付を 1 件として生成）
- [x] 2.2 画像 SHA-256 による冪等性を実装する（既登録と同一ハッシュなら起票も添付もせず既存仕訳を指して返す）
- [x] 2.3 `transactionDate` / `totalAmount` の欠損時に起票せず不足項目を返す経路を実装する
- [x] 2.4 撮影時の文脈（参加人数・相手・用途）を摘要ないしメモへ残す
- [x] 2.5 `POST /skill/receipts/match` を追加する（日付近接＋金額一致で候補提示。起票の経路を持たない）
- [x] 2.6 候補ゼロ・候補複数の応答を実装する（自動選択せず人に委ねる）
- [x] 2.7 既存の期間ゲート・科目解決・未確定勘定への退避が両 API を通ることをテストで固定する
- [x] 2.8 MCP に定型手順「レシートの取込」を追加し、[[receipt-inbox]] の規約（対が揃った件のみ・現金は起票/カードは突合のみ・削除は登録完了の確認後・未登録は消さない）を手順本文に含める
- [x] 2.9 手順が規約を運んでいることをテストで固定する（[[filing]] の転記手順と同型の検査）

## 3. モバイルの足場

- [x] 3.1 `mobile/` に Flutter プロジェクトを作る（アプリ ID `com.susipero.kanean.mobile`）
- [x] 3.2 eslint flat config の `ignores` に `mobile/**` を追加し、`pnpm lint` / `build` / `test` が無影響であることを確認する
- [ ] 3.3 Flutter 用の CI ワークフローを追加する（`mobile/**` のパスフィルタ・analyze ＋ test） — **保留**: リポジトリに CI が1本も無い（`.github/` が存在しない）。TS 側を含む CI の導入は本 change の範囲を超えるので、別途判断が要る
- [x] 3.4 Riverpod と drift を導入し、送信キューと読み取り履歴の 2 テーブルを定義する
- [x] 3.5 生成した JSON Schema から Dart のメタ／status モデルを起こし、1.4 の同じフィクスチャを読む契約テストを書く

## 4. 撮影と文脈付与

- [x] 4.1 スキャン UI を組み込む（iOS=VisionKit / Android=ML Kit Document Scanner を統一 API で扱うプラグイン）
- [x] 4.2 簡易検査を実装する（ブレ・白飛び・見切れの検出 → 理由を示して撮り直しを促す）
- [x] 4.3 利用者が検査を押し切れる経路を実装し、指摘理由をメタに残す
- [x] 4.4 オンデバイス OCR（日本語）で日付と金額を撮影直後に提示する
- [x] 4.5 読み取れなくても撮影を成立させる（当該項目は空のまま送信可能）
- [x] 4.6 現金／カードのワンタップ選択を実装し、未選択のままでは送信できないようにする
- [x] 4.7 用途（事業／按分／私用）・飲食の参加人数と相手・摘要の入力を実装する（いずれも任意）
- [x] 4.8 撮影時に画像の SHA-256 を計算してメタに書く

## 5. 搬送とキュー

- [x] 5.1 搬送層のインタフェースを 1 枚切る（Android を後から足せる形にする）
- [x] 5.2 iOS 実装として iCloud Documents コンテナへの書き出しを実装する（`NSUbiquitousContainers` で iCloud Drive に可視化）
- [x] 5.3 1 件を `{ULID}` を共有する画像とメタの対として書き出す
- [x] 5.4 オフラインで撮影を受理し、送信可能になった時点で送り出すキューを実装する
- [x] 5.5 繰り返し失敗した件を削除せず「要対応」として理由付きで残す

## 6. 履歴と登録の確認

- [x] 6.1 読み取り履歴の一覧を実装する（未送信／送信済み／登録済み／要対応）
- [x] 6.2 status を読み、登録済みの件に日付・金額・科目の要約を表示する
- [x] 6.3 未登録の理由（重複・読み取り不能・期間外・カードの未突合）を表示する
- [x] 6.4 status が返らない件を「送信済み（未登録）」として登録済みと区別する
- [x] 6.5 会計データを取得・表示する経路がアプリに存在しないことを確認する

## 7. 結合と検証

- [x] 7.1 Mac 側で inbox の削除を実装する（登録と証憑保存の完了を確認してからのみ削除・未登録は残す）
- [x] 7.2 対が揃わない件（画像のみ／メタのみ）を処理せず持ち越すことを確認する
- [x] 7.3 現金レシート 1 件を撮影 → 登録 → status 反映 → 中継コピー削除、までを実機で通す — 実機で通した。較正の結果、白飛び判定を作り直し（紙が白いので明るさの割合では判定不能）
- [x] 7.4 カードレシート 1 件で、起票されず既存明細へ証憑としてのみ添付されることを確認する — 実機で通した（候補ゼロ・起票なし・中継コピーは残す）
- [x] 7.5 同じレシートを 2 回送っても仕訳と添付が重複しないことを確認する — 実機の画像で確認（2回目は duplicate で既存仕訳を指す）
- [x] 7.6 削除の前に処理を中断し、再開しても二重起票しないことを確認する — 実機で確認。カードの1件が未登録のまま inbox に残る状態を作り、再処理しても起票されないことを確認（現金側は同一画像の duplicate を 7.5 で実証）
- [x] 7.7 画像の渡り先（外部の AI サービスを経由すること）の明示を UI に入れる
- [x] 7.8 `openspec validate --changes receipt-capture-app --strict` と `pnpm test` / `lint` / `typecheck` を通す
