## 1. パッケージ足場

- [x] 1.1 `packages/mcp` を作成（TypeScript strict / ESM / `.js` 付き import / vitest。turbo と pnpm workspace に登録）
- [x] 1.2 `@modelcontextprotocol/sdk` を追加し、stdio サーバが起動してツール0本で接続できることを確認
- [x] 1.3 `packages/server` と `packages/shared` の型を参照できるようにする（実装は import しない＝会計ロジックを持ち込まない）
      → server に型専用の `./types` サブパスを追加（`export type` 再輸出のみ＝`dist/types.js` は空）。
        mcp 側は devDependency で参照し、実装 import をテストで禁止。

## 2. 接続層（ローカルアプリへの到達）

- [x] 2.1 接続先の解決を実装（既定はソケットのみ＝`KANEAN_SOCKET` → 既定パス。TCP は `KANEAN_BASE_URL` を明示したときだけ。`localhost` は使わない）
- [x] 2.2 unix socket / TCP のどちらでも同じ呼び出しになる HTTP クライアントを実装
- [x] 2.3 ソケット不在＝アプリ未起動として判別し、専用の結果型で返す
      （`ENOENT` / `ECONNREFUSED`＝異常終了の残骸 / `ENOTSOCK`＝別種のファイル、いずれも未起動）
- [x] 2.4 確認を得てからアプリを起動し、到達可能になるまで有限時間待って**1回だけ**再試行する経路を実装
      （確認は MCP の elicitation。非対応クライアントでは勝手に起動せず未起動として返す）
- [x] 2.5 起動を断られた場合・待ちが尽きた場合・処理中にアプリが終了した場合をそれぞれ区別して報告する
- [x] 2.6 テスト: 経路解決 / 未起動判別 / 再試行が1回で止まること / 明示指定が無いとき TCP へ接続を試みないこと

## 3. 読み取りツール

- [x] 3.1 `kanean_get_context`（帳簿・会計年度・アプリモード・承認待ち件数を合成して返す）
- [x] 3.2 試算表 / 損益計算書 / 貸借対照表 のツール（期間指定を受ける）
- [x] 3.3 総勘定元帳 / 補助元帳 のツール（`get_ledger` 1本に集約。`subAccountId` で補助元帳）
- [x] 3.4 仕訳検索のツール
- [x] 3.5 科目残高のツール（試算表の合成で「指定日時点の指定科目」を返す。サーバに新エンドポイントを足さない）
- [x] 3.6 納税予測のツール
- [x] 3.7 連携サービス一覧のツール
- [x] 3.8 テスト: 集計に `confirmed` のみが入り、未確定は件数として別に返ること

## 4. 帳簿解決と誘導（nextActions）

- [x] 4.1 帳簿の解決を既存規約へ委譲（1冊なら指定不要 / 2冊以上は選択肢を返して中断）
- [x] 4.2 前提不足（会計年度なし・連携サービス未登録 等）を選択肢付きで返す共通の形を用意
- [x] 4.3 除外・重複・未確定の件数を結果に必ず含める（黙って落とさない）
- [x] 4.4 承認・編集が要る結果に `kanean://` の深いリンクを添える（`kanean://local/?tab=<TabKey>`）
- [x] 4.5 テスト: 帳簿2冊での中断と選択肢返却 / 会計年度なしで空の帳票を返さないこと

## 5. 書き込みツール（限定）

- [x] 5.1 `kanean_register_linked_service`（選択肢を提示して確認を得てから登録）
- [x] 5.2 仕訳の確定・取消・削除、マスタ・期首残高・税区分の編集を行うツールが**存在しない**ことをテストで固定
- [x] 5.3 露出ツール数の上限（目安12・最大20）を超えていないことをテストで固定

## 6. 定型手順（prompts）

- [x] 6.1 「今月の状況」prompt
- [x] 6.2 「決算前チェック」prompt
- [x] 6.3 ツール説明を最小限に保つ（順序の前提のみ記載。手順は prompts と `nextActions` に置く）

## 7. デスクトップ側（プロトコルハンドラ）

- [x] 7.1 `app.setAsDefaultProtocolClient('kanean')` を登録し、OS からのリンク受理を既存の `protocol.handle`（アプリ内配信）と切り分ける
- [x] 7.2 起動中/未起動それぞれでリンクを受け、ウィンドウを前面に出して該当画面へ遷移させる
      （macOS は `open-url`、他は起動引数。web 側は `?tab=` を初期タブとして読む）
- [x] 7.3 未知の経路は初期画面へフォールバックし、異常終了しない
- [x] 7.4 リンクが画面遷移のみを行い、変更操作を実行しないことをテストで固定

## 8. 設定画面の導線（web）

- [x] 8.1 設定画面に「Claude Desktop と連携」パネルを追加（`components/AiLinkPanel.tsx`）
- [x] 8.2 バンドルを書き出して保存場所を提示し、導入手順（Extensions へドロップ）を表示
- [x] 8.3 連携後にできること・できないこと（承認はアプリで行う）を明示
- [x] 8.4 未設定 / 設定済みを区別して示す
      （デスクトップ版でなければ `/api/desktop/*` が 404 →「アプリから操作してください」を表示）
      ※ web は component テストの基盤（testing-library）を持たないためパネル単体テストは無い。
        書き出し・404 分岐の実挙動は `packages/desktop` 側で9件テスト済み。

## 9. パッケージングと配布

- [x] 9.1 `manifest.json` を作成し、`mcpb` CLI で `.mcpb` を生成するビルドスクリプトを追加
- [x] 9.2 サイズを記録（**104KB**）。**ランタイムは同梱しない** — Claude Desktop が Node を同梱しており
      利用者側に Node は不要と確認できたため（design D8 を実測で更新）。依存は esbuild で1ファイルへ
- [x] 9.3 `electron-builder` の `extraResources` に `.mcpb` を追加し、`/api/desktop/*` から書き出す
- [x] 9.4 素の Claude Desktop（Node 未導入のマシン想定）へドロップして疎通を確認
      （実機で導入・接続・ツール呼び出しまで確認済み。導入時にクライアント側の権限確認が出るが
        「常に許可」で継続できる。パック済み単体の起動は `bundle` が毎回自己検証する）

## 10. 検証と docs

- [~] 10.1 明示指定した開発時 TCP 経路とソケット経路で、同じツールが同じ結果を返すことを確認
      → **socket 経路のみ実機確認**。TCP は開発時に明示指定したときだけの経路で、配布物は通らない。
        経路解決と socket 経路の呼び出しはテストで固定済み。TCP 経路の実 DB 同値確認は未実施。
- [x] 10.2 「7月末の売掛金は？」「今月の状況は？」を実際に会話させ、ツール選択と誘導が成立するか確認
- [x] 10.3 発話に「kanean」を含めなくてもツールが選ばれるかを確認し、結果を design の Open Questions へ反映
      → **接頭辞なしで選ばれた**（「7月末の売掛金は？」に "kanean" を含まない）。design へ反映済み
- [x] 10.4 `docs/architecture.md` §12.0 の図に MCP 経路を追記
- [x] 10.5 `docs/roadmap.md` の「次の change の MCP サーバ」を実装済みとして更新
- [x] 10.6 `pnpm lint` / `pnpm typecheck` / `pnpm test` を通す（788 tests green）
