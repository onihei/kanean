## 1. 同梱版を配布物へ載せる（D5）

- [x] 1.1 バンドル生成が `.mcpb` と並べて版だけのファイルを出力する（出所は `manifest.json` のまま。既存の版一致検査は残す）
- [x] 1.2 `electron-builder.yml` の `extraResources` にその版ファイルを追加し、パッケージ後に配布物へ入ることを確認する
- [x] 1.3 デスクトップが同梱版を読み、開発時（同梱物が無い）は「不明」として扱えることをテストで固定する（D7）

## 2. クライアントが版を名乗る（D4）

- [x] 2.1 アプリ API を呼ぶ全経路のヘッダにクライアント名と版を載せる（ツールごとの実装に散らさない）
- [x] 2.2 版の出所を `manifest.json` と一致させ、ずれたらビルドで気づける状態を保つ
- [x] 2.3 ヘッダが全ツールの呼び出しに載ることをテストで固定する

## 3. 一致しないクライアントを拒否する（D1・D2・D3）

- [x] 3.1 アプリ生成時に同梱版を受け取り、判明しているときだけ検査する経路を用意する（D7）
- [x] 3.2 `/api/*` の前段で版を検査し、一致しなければ `426` と `{ error: { code, message } }` を返す
- [x] 3.3 メッセージに使用中の版・同梱の版・書き出して入れ直す手順を含める
- [x] 3.4 `/health` が検査の対象外であることをテストで固定する（**破ると誤った案内に化ける**。回帰の要）
- [x] 3.5 版を名乗らない要求が一致しない版と同じ拒否になることをテストで固定する
- [x] 3.6 拒否された書き込み要求で帳簿が変わらないことをテストで固定する

## 4. 到達を記録する（D6）

- [x] 4.1 `app_settings` に最後の到達時刻と版を記録する読み書きを足す
- [x] 4.2 検査を通った要求と拒否した要求の**両方**で記録する
- [x] 4.3 記録が無い状態を「到達を観測していない」として返せる形にする（「未導入」とは区別する）
- [x] 4.4 連携の状態を返す API を足す（記録の有無・最後の版・同梱版・一致するか）
- [x] 4.5 帳簿を切り替えても同じ記録が参照されることをテストで固定する

## 5. ホーム画面の案内（web-app spec）

- [x] 5.1 ホームのセットアップ案内で連携の状態を取得する
- [x] 5.2 記録が無いとき「連携がまだ確認できていません」と、一度試す導線を出す（**導入の有無は書かない**）
- [x] 5.3 版が一致しないとき、観測した版と時点を添えて設定画面の書き出し導線へ誘導する
- [x] 5.4 一致する到達があるとき何も出さない
- [x] 5.5 帳簿の成立に関わる既存3行の後ろに置く
- [x] 5.6 同梱版が不明なとき（開発時）は案内を出さない（D7）
- [x] 5.7 3状態の出し分けをテストで固定する

## 6. 実機で確かめる

- [x] 6.1 版を名乗らない現行バンドルを Claude Desktop に入れたまま更新後のアプリを起動し、質問して**拒否の案内が Claude の画面に出る**ことを確認する（本変更の目的そのもの）
- [x] 6.2 その状態でアプリが「起動していません」と案内されないことを確認する（D2 の実機確認）
- [x] 6.3 設定画面から書き出して入れ直し、案内が消えて通常どおり答えることを確認する
- [x] 6.4 ホームの3状態（未観測 → 一致しない版 → 一致）が実際に遷移することを確認する
- [x] 6.5 `pnpm test` / `pnpm lint` / `pnpm typecheck` を通す
