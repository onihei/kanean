# データ配置（SQLite）。開発は ./data、本番は ~/data/kanean 等の永続パス。
# コード配下には置かないこと（deploy が dist を置換するため消える）。
DATA_DIR=./data

# サーバ
# 待ち受けは常に 127.0.0.1 固定（変更不可）。認証を持たない代わりに、ループバック限定
# バインドが唯一の防壁のため、外部公開へ切り替える設定は意図的に提供しない。
PORT=10140

# バックアップ用メール（任意・将来）
SMTP_URL=
BACKUP_MAIL_TO=
