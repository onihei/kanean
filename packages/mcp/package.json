{
  "name": "@kanean/mcp",
  "license": "AGPL-3.0-only",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "kanean-mcp": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "bundle": "node scripts/bundle.mjs",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src/"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "zod": "^4.5.4"
  },
  "//devDependencies": "@kanean/server・@kanean/shared・@kanean/acquisition は **型のためだけ**に置く（実装は import しない＝会計ロジックを持ち込まない）。acquisition は server/types が再輸出する JobView 等の参照先（Calibration/StoredDiagnostic/Sel）の解決に要る。devDependencies なのは配布バンドルに同梱させないため。",
  "devDependencies": {
    "@anthropic-ai/mcpb": "^2.1.2",
    "@kanean/acquisition": "workspace:*",
    "@kanean/server": "workspace:*",
    "@kanean/shared": "workspace:*",
    "@types/node": "^24.13.3",
    "esbuild": "^0.28.2",
    "vitest": "^4.1.11"
  }
}
